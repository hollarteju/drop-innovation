{
  "build": {
    "env": {
      "NODE_ENV": "production"
    },
    "outputDirectory": "lesellify/dist" // Change this to your actual build output directory
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "lesellify/dist/$1" // Adjust according to your build output
    }
  ]
}
